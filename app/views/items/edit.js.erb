var item = $("<%= escape_javascript(render(partial: 'items/edit_form', locals: { item: @item })) %>");
$('#item_<%= @item.id %>').replaceWith(item);
<% if @item.image? %>
$('#item_<%= @item.id %>_form').itemEditImageForm({
    'url' : '<%= list_item_path(@item.list, @item) %>'
});
<% end %>
