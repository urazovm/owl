.header_logo.col-sm-2
  = link_to 'OWL', lists_path

.header_description.col-sm-7
  %p Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed tincidunt est vel.

.header_controls.col-sm-3
  = link_to 'Create a new list', new_list_path, class: 'btn btn-primary'
  %p
    - if signed_in?
      = link_to current_user.login, user_path(current_user, r: 1)
      %span /
      = link_to 'logout', destroy_user_session_path, method: :delete
    - else
      = link_to 'register', new_user_registration_path
      %span /
      = link_to 'login', new_user_session_path(r: 1)
