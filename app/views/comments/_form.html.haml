= minimal_form_for [comment.list, comment], html: {class: 'hidden', role: 'form'}  do |f|
  = f.input :message, as: :text
  = f.button :submit, t('.submit'), class: 'btn btn-primary btn-full hidden'
  %hr
